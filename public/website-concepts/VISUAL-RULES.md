# BCN visual rules (canonical copy lives in BCN-Track)

The full rules file is:

**`BCN-Track/projects/website-job/bcn-visual-rules.md`**

(open that path in the repo for typography, imagery hierarchy, CTA rules, and pre-ship checks)

Shared CSS polish for sprint pages: **`bcn-visual-system.css`** (load with `class="bcn-sprint-page"` on `<body>`).
